dataset:
  # Filled by scripts/train/cartpole_wm.py as wm.history_size + train_horizon
  # unless explicitly overridden from the command line.
  num_steps: null
  frameskip: 1
  name: cartpole_expert_worldmodel
  keys_to_load:
    - pixels
    - action
    - state
  keys_to_cache:
    - action
    - state
